To install this package, either type the following in the current
directory (note you may need administrator privileges to do so)::

    python setup.py install

Or perform a Subversion checkout from a location on your Python import
path::

    svn co http://python-pownce-api.googlecode.com/svn/trunk/pownce.py

