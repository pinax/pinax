This is a simple Python wrapper around the Pownce
(<http://pownce.com/>) API; for documentation, either open a Python
interpreter and type::

    >>> import pownce
    >>> help(pownce)

Or at a command line, type::

    pydoc pownce
