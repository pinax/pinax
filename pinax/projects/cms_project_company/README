This project is based on cms_project_holidayhouse but shows how things can
be changed at the project level, not just in the content itself.

To use sample content you can run loaddata after syncdb::

    python manage.py loaddata sample_content.jsonfk
